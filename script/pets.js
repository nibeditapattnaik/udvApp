















/*var pet = angular.module("petmodule",[]);

pet.controller("petcontroller", function($scope,$http){
    $http.get('json/pets.json').then(function(response){
        $scope.petData = response.data;
        console.log($scope.petData);
        console.log(response);
    });
    $scope.removeName = function(row){
        $scope.petData.splice($scope.petData.indexOf(row),1);
    }
});
*/